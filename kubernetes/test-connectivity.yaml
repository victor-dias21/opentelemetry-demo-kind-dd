apiVersion: v1
kind: Pod
metadata:
  name: test-connectivity
  namespace: otel-demo
spec:
  containers:
  - name: test
    image: busybox:1.35
    command: ['sh', '-c', 'nslookup registry-1.docker.io && wget -O- https://registry-1.docker.io/v2/ || echo "Conectividade falhou"']
  restartPolicy: Never
